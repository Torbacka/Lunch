---
status: partial
phase: 07.1-multi-office-ux-places-autocomplete-install-always-prompt-pe
source: [07.1-VERIFICATION.md]
started: 2026-04-15T00:00:00Z
updated: 2026-04-15T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Browser install form — Places autocomplete widget
expected: Visiting `/slack/setup` renders a form with an address input. Typing a partial address shows live suggestions from `/places/autocomplete`. Selecting a suggestion populates the hidden `place_id` field. Submitting the form creates a `workspace_locations` row with `is_default=true` and starts restaurant seeding.
result: [pending]

### 2. Always-prompt in unbound channel — single-office workspace
expected: In a workspace with exactly one office, running `/lunch` in an unbound channel still shows the channel-location prompt (NOT auto-bind). A second `/lunch` in the same channel after binding proceeds silently to the poll.
result: [pending]

### 3. Add-office modal from /lunch prompt — end-to-end
expected: Clicking "Add office" on the channel-location prompt opens a modal with a Places external-select. Typing fetches predictions. Submitting creates a `workspace_locations` row, binds the channel to it, and posts the lunch poll in that channel.
result: [pending]

### 4. App Home Offices rendering and admin gate
expected: App Home shows an Offices section listing all offices with is_default indicator. Admin users see Rename / Set Default / Delete buttons per row plus an Add Office button. Non-admin users see only the Add Office button (no per-row controls). Rename, Set Default, Delete operations work end-to-end through modals.
result: [pending]

### 5. Delete cascade observed from Slack
expected: Deleting an office that was bound to one or more channels causes those channels to revert to the always-prompt state on the next `/lunch` invocation. The deleted office disappears from the App Home list and the Add Office modal's external_select.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
