---
phase: 07.2
plan: 07
type: execute
wave: 5
depends_on: [07.2-02, 07.2-03, 07.2-04, 07.2-05, 07.2-06]
files_modified:
  - tests/conftest.py
  - tests/test_recommendation_db.py
  - tests/test_db.py
  - tests/test_scheduler.py
  - tests/test_app_home.py
  - tests/test_migration_009.py
  - tests/test_slack_actions.py
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8]
must_haves:
  truths:
    - "G-01 regression test exists and asserts channel-bound pools do not overlap across offices"
    - "G-02 regression test exists and asserts two channels in the same office accumulate divergent restaurant_stats rows"
    - "Migration 009 smoke test exists and asserts post-upgrade invariants (restaurant_stats PK, polls.slack_channel_id NOT NULL, channel_schedules populated from legacy workspaces schedule rows)"
    - "Scheduler test exists and asserts load_all_schedules produces job ids matching `poll_{team_id}_{channel_id}`"
    - "App Home test exists and asserts the rendered view contains a per-channel schedule row for each channel_schedules entry"
    - "Channel picker test asserts list_bot_channels excludes non-member channels"
    - "upsert_suggestion called with channel_id=None raises"
  artifacts:
    - path: "tests/test_recommendation_db.py"
      provides: "G-01 + G-02 regression tests + updated existing tests"
    - path: "tests/test_migration_009.py"
      provides: "Migration 009 upgrade smoke test"
    - path: "tests/test_scheduler.py"
      provides: "Channel-keyed scheduler test"
    - path: "tests/test_app_home.py"
      provides: "Per-channel schedule rendering test"
  key_links:
    - from: "test_get_candidate_pool_office_isolation"
      to: "db_client.get_candidate_pool"
      via: "two offices, two channels, disjoint pools"
      pattern: "assert.*set.*isdisjoint\\|assert.*not in"
---

<objective>
Write the regression + integration test suite that proves every acceptance criterion for Phase 07.2 holds. Covers G-01 isolation, G-02 divergence, migration 009 invariants, scheduler job keying, App Home rendering, and channel picker filtering.

Purpose: Makes G-01 and G-02 permanently non-regressable; covers the restructure surface.
Output: New + updated tests under `tests/`.
</objective>

<execution_context>
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md
@tests/conftest.py
@tests/test_recommendation_db.py
@tests/test_db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test fixtures + G-01 / G-02 / Beta fallback / cascade regression tests</name>
  <files>tests/conftest.py, tests/test_recommendation_db.py, tests/test_db.py</files>
  <read_first>
    - tests/conftest.py full (existing fixtures: clean_all_tables_with_stats, workspace_a, workspace_b, tenant_connection, sample_restaurant)
    - tests/test_recommendation_db.py full (existing helpers `_insert_workspace`, `_insert_restaurant`, `_insert_poll`, `_insert_poll_option`, `_insert_vote`; tests at lines 108, 128, 151, 176 that must be rewritten)
    - tests/test_db.py (upsert_suggestion call sites ~lines 55, 95 per research)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md sections "Test Strategy", "Validation Architecture"
  </read_first>
  <behavior>
    - New fixture helpers `_insert_workspace_location(conn, team_id, name, lat_lng, is_default=True)` returns id; `_insert_channel_location(conn, team_id, channel_id, location_id)`; `_insert_channel_schedule(conn, team_id, channel_id, time, tz, weekdays)`.
    - `_insert_restaurant` now takes required `location_id`.
    - `_insert_poll` now takes required `slack_channel_id`.
    - test_get_candidate_pool_office_isolation: seeds 1 workspace, 2 offices (A, B), 2 channels (C_A bound to A, C_B to B), 2 restaurants per office. Asserts `set(get_candidate_pool(today, 'C_A')).isdisjoint(set(get_candidate_pool(today, 'C_B')))` AND both pools non-empty.
    - test_restaurant_stats_divergence_same_office: 1 workspace, 1 office, 2 channels (C_1, C_2) both bound. Calls update_restaurant_stats(C_1, r1, alpha_inc=5, beta_inc=0, team_id) and update_restaurant_stats(C_2, r1, alpha_inc=0, beta_inc=5, team_id). Reads both rows and asserts `row_c1['alpha'] > row_c2['alpha']` and `row_c1['beta'] < row_c2['beta']`.
    - test_get_candidate_pool_new_channel_prior: seed restaurant with location_id, bind channel, no restaurant_stats row. Assert returned row has alpha==1.0, beta==1.0, times_shown==0.
    - test_delete_office_cascades_restaurants_and_stats: create office/restaurant/stats; DELETE FROM workspace_locations WHERE id=...; assert restaurants row and restaurant_stats row both gone.
    - test_upsert_suggestion_requires_channel_id: call upsert_suggestion(today, 1, 'T1') → expect TypeError OR call with slack_channel_id=None → expect DB IntegrityError (NOT NULL violation).
  </behavior>
  <action>
1. **Extend `tests/conftest.py`**:
   - In `clean_all_tables_with_stats`, add `channel_schedules` to the TRUNCATE list (workspace_locations and channel_locations should already cascade from workspaces; verify by running the test DB and adjust only if needed).
   - No other changes.

2. **Extend `tests/test_recommendation_db.py`**:
   - Add module-level helpers:
```python
def _insert_workspace_location(conn, team_id, name='Main', lat_lng=None, is_default=True):
    lat_lng = lat_lng or {'lat': 59.0, 'lng': 18.0}
    cur = conn.cursor()
    cur.execute(
        "INSERT INTO workspace_locations (team_id, name, latitude, longitude, is_default) "
        "VALUES (%s, %s, %s, %s, %s) RETURNING id",
        (team_id, name, lat_lng['lat'], lat_lng['lng'], is_default),
    )
    return cur.fetchone()[0]

def _insert_channel_location(conn, team_id, channel_id, location_id):
    cur = conn.cursor()
    cur.execute(
        "INSERT INTO channel_locations (team_id, channel_id, location_id) VALUES (%s, %s, %s)",
        (team_id, channel_id, location_id),
    )
```
   - Update `_insert_restaurant(conn, team_id, place_id, name, location_id, rating=4.5)` to take `location_id` as a required arg and include it in the INSERT column list.
   - Update `_insert_poll(conn, team_id, poll_date, slack_channel_id)` to take `slack_channel_id` as a required arg and include it in the INSERT.
   - Rewrite the four existing tests at lines ~108, ~128, ~151, ~176 to use the new signatures; every seed must first create a workspace_location, then a channel_location, then a restaurant tagged with that location_id.

   - Add the four new tests described in `<behavior>`:
     - `test_get_candidate_pool_office_isolation` — uses the two-office fixture, asserts disjoint pools, each non-empty with 2 restaurants.
     - `test_restaurant_stats_divergence_same_office` — calls `db_client.update_restaurant_stats` twice with divergent increments, asserts the two resulting rows differ.
     - `test_get_candidate_pool_new_channel_prior` — asserts Beta(1,1) fallback.
     - `test_delete_office_cascades_restaurants_and_stats` — DELETE the office row, assert both downstream rows gone.

3. **Extend `tests/test_db.py`**:
   - Add `test_upsert_suggestion_requires_channel_id` asserting that calling `db_client.upsert_suggestion(today, restaurant_id, workspace_id)` without the fourth arg raises `TypeError`.
   - Fix any existing test that still calls `upsert_suggestion` with the old signature — add `slack_channel_id='C123'` to every call.
  </action>
  <verify>
    <automated>pytest tests/test_recommendation_db.py tests/test_db.py -x --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_get_candidate_pool_office_isolation" tests/test_recommendation_db.py` returns 1
    - `grep -c "def test_restaurant_stats_divergence_same_office" tests/test_recommendation_db.py` returns 1
    - `grep -c "def test_get_candidate_pool_new_channel_prior" tests/test_recommendation_db.py` returns 1
    - `grep -c "def test_delete_office_cascades_restaurants_and_stats" tests/test_recommendation_db.py` returns 1
    - `grep -c "def _insert_workspace_location" tests/test_recommendation_db.py` returns 1
    - `grep -c "def _insert_channel_location" tests/test_recommendation_db.py` returns 1
    - `grep -c "def test_upsert_suggestion_requires_channel_id" tests/test_db.py` returns 1
    - `grep -c "isdisjoint" tests/test_recommendation_db.py` >= 1
    - `pytest tests/test_recommendation_db.py tests/test_db.py -x` exits 0 (after plan 07.2-08 applies migration)
  </acceptance_criteria>
  <done>Regression suite for G-01, G-02, cascade, and NOT NULL contract exists and passes post-migration.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 009 smoke test + scheduler test + App Home test + channel picker test</name>
  <files>tests/test_migration_009.py, tests/test_scheduler.py, tests/test_app_home.py, tests/test_slack_actions.py</files>
  <read_first>
    - tests/conftest.py (connection/pool patterns, how migrations run against the test DB)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Validation Architecture (Nyquist — expanded)"
    - lunchbot/services/scheduler_service.py (post plan 07.2-04 shape)
    - lunchbot/services/app_home_service.py (post plan 07.2-06 shape)
  </read_first>
  <action>
1. **tests/test_migration_009.py** (NEW):
   - Assumes the test DB has already run `alembic upgrade head` (the global pytest fixture does this).
   - `test_restaurants_location_id_not_null`: query information_schema.columns; assert column `location_id` on table `restaurants` has `is_nullable = 'NO'`.
   - `test_restaurant_stats_primary_key`: query information_schema.table_constraints + key_column_usage; assert PK of `restaurant_stats` is exactly `(channel_id, restaurant_id)` and table has NO column named `workspace_id`.
   - `test_polls_slack_channel_id_not_null`: assert `polls.slack_channel_id` is_nullable = 'NO'.
   - `test_channel_schedules_exists`: SELECT 1 FROM channel_schedules; asserts no exception. PK check: assert PK is `(team_id, channel_id)`.
   - `test_workspaces_schedule_columns_dropped`: query information_schema.columns; assert none of `poll_schedule_time`, `poll_schedule_timezone`, `poll_schedule_weekdays`, `poll_channel` exist on `workspaces`.

2. **tests/test_scheduler.py** (NEW or extend existing):
   - `test_load_all_schedules_creates_channel_keyed_jobs`:
     - Seed a workspace + channel_schedules row `(team_id='T1', channel_id='C1', schedule_time=time(12,0), schedule_timezone='UTC', schedule_weekdays='mon,tue')`.
     - Call `scheduler_service.load_all_schedules()`.
     - Assert `scheduler_service._scheduler.get_job('poll_T1_C1')` is not None.
   - `test_update_schedule_job_upserts_job`:
     - Call `update_schedule_job('T1', 'C1', time(9,0), 'UTC', 'wed')` and assert the job's trigger matches.
   - `test_remove_schedule_job_removes_job`:
     - Add then remove; assert `get_job` returns None.
   - Use whatever pytest marker / app context fixture the project already uses for scheduler tests; if none exists, wrap each test in an `app.app_context()`.

3. **tests/test_app_home.py** (NEW or extend existing):
   - `test_build_home_view_renders_per_channel_schedules`:
     - schedules = `[{'channel_id': 'C1', 'schedule_time': time(12,0), 'schedule_timezone': 'UTC', 'schedule_weekdays': 'mon,tue'}, {'channel_id': 'C2', 'schedule_time': time(13,0), 'schedule_timezone': 'UTC', 'schedule_weekdays': 'wed'}]`
     - `view = app_home_service.build_home_view({'poll_size': 5}, True, [], schedules)`
     - Flatten `view['blocks']` to a JSON string and assert both `'<#C1>'` and `'<#C2>'` appear, AND `'open_schedule_channel_modal'` appears, AND the legacy `'poll_channel'` section text does NOT appear.
   - `test_build_home_view_no_workspace_schedule_sections`:
     - Pass empty schedules; assert none of the rendered block JSON contains the strings `'Poll Channel'` (the old section header) or `'Poll Schedule'` (the old section header).

4. **tests/test_slack_actions.py** (extend, or create):
   - `test_list_bot_channels_filters_to_members`:
     - Monkeypatch `slack_client.session.get` (or whatever HTTP layer the client uses) to return a fake `users.conversations` response with exactly 3 channels (e.g. `{'ok': True, 'channels': [{'id':'C1','name':'a'},{'id':'C2','name':'b'},{'id':'C3','name':'c'}], 'response_metadata': {'next_cursor': ''}}`).
     - Call `slack_client.list_bot_channels('T1')`.
     - Assertion 1 (count): the returned channel list has exactly 3 entries — `assert len(channels) == 3` — proving every entry in the response was returned.
     - Assertion 2 (endpoint): inspect the recorded mock call args and assert the outgoing request URL contains the literal substring `'users.conversations'` AND does NOT contain `'conversations.list'`. This proves D-16: `users.conversations` server-side-filters to bot-member channels by Slack API contract, so any non-member channel would be excluded before it reaches the caller.
     - Assertion 3 (shape): the return value is a `(channels, next_cursor)` tuple with `next_cursor == ''`.
   - `test_add_office_modal_seeds_with_location`:
     - Monkeypatch `seed_service._seed_office_restaurants` with a recorder.
     - Dispatch a fake view_submission payload for CALLBACK_ADD_OFFICE with a valid place_id.
     - Assert the recorder was called once with the newly created location_id.
  </action>
  <verify>
    <automated>pytest tests/test_migration_009.py tests/test_scheduler.py tests/test_app_home.py tests/test_slack_actions.py -x --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_migration_009.py` succeeds
    - `grep -c "def test_restaurants_location_id_not_null\|def test_restaurant_stats_primary_key\|def test_polls_slack_channel_id_not_null\|def test_channel_schedules_exists\|def test_workspaces_schedule_columns_dropped" tests/test_migration_009.py` returns 5
    - `grep -c "def test_load_all_schedules_creates_channel_keyed_jobs" tests/test_scheduler.py` returns 1
    - `grep -c "poll_T1_C1\|poll_{team_id}_{channel_id}" tests/test_scheduler.py` >= 1
    - `grep -c "def test_build_home_view_renders_per_channel_schedules" tests/test_app_home.py` returns 1
    - `grep -c "open_schedule_channel_modal" tests/test_app_home.py` >= 1
    - `grep -c "def test_list_bot_channels_filters_to_members\|def test_add_office_modal_seeds_with_location" tests/test_slack_actions.py` >= 1
    - `grep -c "users.conversations" tests/test_slack_actions.py` >= 1
    - `grep -c "conversations.list" tests/test_slack_actions.py` >= 1  (negative assertion string present in the test)
    - `grep -c "assert len(channels) == 3" tests/test_slack_actions.py` >= 1
    - `pytest tests/test_migration_009.py tests/test_scheduler.py tests/test_app_home.py tests/test_slack_actions.py -x` exits 0 (after plan 07.2-08 applies migration)
  </acceptance_criteria>
  <done>Full regression surface for Phase 07.2 is covered with grep-verifiable assertions.</done>
</task>

</tasks>

<verification>
pytest + grep acceptance above.
</verification>

<success_criteria>
All SC-1..SC-8 have at least one automated test that encodes the acceptance criterion.
</success_criteria>

<output>
Create `.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-07-SUMMARY.md`
</output>
