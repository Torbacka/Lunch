---
phase: 07.2
plan: 03
type: execute
wave: 3
depends_on: [07.2-01, 07.2-02]
files_modified:
  - lunchbot/services/seed_service.py
  - lunchbot/services/emoji_service.py
  - lunchbot/blueprints/setup.py
  - lunchbot/blueprints/slack_actions.py
  - lunchbot/blueprints/polls.py
autonomous: true
requirements: [SC-4, SC-5]
must_haves:
  truths:
    - "A shared helper `_seed_office_restaurants(team_id, location_id, lat_lng)` exists in lunchbot/services/seed_service.py"
    - "The install flow (setup.py) invokes the shared helper with the new workspace_locations.id"
    - "The add-office Slack modal (slack_actions.py CALLBACK_ADD_OFFICE path) invokes the same helper with the newly created location_id"
    - "emoji_service.search_and_update_emoji accepts location_id and forwards it to save_restaurants"
    - "places_client search path (find_suggestions) tags saved restaurants with the channel's bound office location_id"
  artifacts:
    - path: "lunchbot/services/seed_service.py"
      provides: "Shared seeding helper used by both install and add-office paths"
      contains: "_seed_office_restaurants"
  key_links:
    - from: "slack_actions.py add-office modal submission"
      to: "seed_service._seed_office_restaurants"
      via: "function call after create_workspace_location"
      pattern: "_seed_office_restaurants\\("
---

<objective>
Extract install-flow restaurant seeding into a shared helper (D-17) and call it from both the install form AND the add-office Slack modal. Thread `location_id` through the entire seed pipeline so every restaurant row is tagged with its owning office.

Purpose: Without this, new offices created via the Slack add-office modal have empty candidate pools, masking G-01's fix.
Output: Shared seed helper + updated call sites.
</objective>

<execution_context>
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md
@lunchbot/blueprints/setup.py
@lunchbot/blueprints/slack_actions.py
@lunchbot/services/emoji_service.py
@lunchbot/blueprints/polls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared seed helper and wire install flow</name>
  <files>lunchbot/services/seed_service.py, lunchbot/services/emoji_service.py, lunchbot/blueprints/setup.py</files>
  <read_first>
    - lunchbot/blueprints/setup.py lines 1-100 (current `_seed_restaurants` internal helper and its call site)
    - lunchbot/services/emoji_service.py full (current `search_and_update_emoji(location)`, line 47; `db_client.save_restaurants` call at line 62)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Seed Path Integration Points — exact edits"
  </read_first>
  <action>
1. **Create `lunchbot/services/seed_service.py`**:
```python
"""Shared office restaurant seeding helper.

Used by both the install flow (setup.py) and the add-office Slack modal
(slack_actions.py) to populate workspace_locations with a tagged candidate pool.
"""
from lunchbot.services import emoji_service


def _seed_office_restaurants(app, team_id, location_id, lat_lng):
    """Seed restaurants for a newly created workspace_locations row.

    Args:
        app: Flask app (for app_context in background thread callers)
        team_id: workspaces.team_id (tenant)
        location_id: workspace_locations.id (must exist)
        lat_lng: dict with 'lat' and 'lng' (Places search origin)
    """
    with app.app_context():
        emoji_service.search_and_update_emoji(lat_lng, location_id, team_id)
```
Snake_case, no type hints. Docstring only.

2. **Edit `lunchbot/services/emoji_service.py`**:
   - Change `search_and_update_emoji(location)` signature to `search_and_update_emoji(location, location_id, team_id=None)`.
   - Find the `db_client.save_restaurants({'results': results})` call (~line 62) and change it to `db_client.save_restaurants({'results': results}, location_id)`.
   - Do not touch the emoji-update-by-place_id path; only the save path needs the new arg.

3. **Edit `lunchbot/blueprints/setup.py`**:
   - Delete the internal `_seed_restaurants` helper (if it exists as a module-local) OR replace its body with a call to `seed_service._seed_office_restaurants(app, team_id, location_id, lat_lng)`.
   - Import: `from lunchbot.services import seed_service`.
   - After `create_workspace_location(...)` returns a row with `id`, call `seed_service._seed_office_restaurants(current_app._get_current_object(), team_id, row['id'], lat_lng)` (preserve whatever background-thread pattern the install flow already uses — keep it in a thread if it was one before).
   - Remove any remaining direct call to `search_and_update_emoji` that does not pass `location_id`.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(p).read()) for p in ['lunchbot/services/seed_service.py','lunchbot/services/emoji_service.py','lunchbot/blueprints/setup.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lunchbot/services/seed_service.py` succeeds
    - `grep -c "def _seed_office_restaurants" lunchbot/services/seed_service.py` returns 1
    - `grep -c "from lunchbot.services import seed_service" lunchbot/blueprints/setup.py` returns 1
    - `grep -c "def search_and_update_emoji" lunchbot/services/emoji_service.py` returns 1 AND the signature includes `location_id`
    - `grep "save_restaurants({'results': results}" lunchbot/services/emoji_service.py` shows a second argument (location_id) — `grep -c "save_restaurants({'results': results}, location_id)" lunchbot/services/emoji_service.py` returns 1
    - All three files ast.parse successfully
  </acceptance_criteria>
  <done>Helper exists; install flow uses it; emoji_service propagates location_id.</done>
</task>

<task type="auto">
  <name>Task 2: Wire add-office modal + find_suggestions search path + polls.py seed helper call</name>
  <files>lunchbot/blueprints/slack_actions.py, lunchbot/blueprints/polls.py</files>
  <read_first>
    - lunchbot/blueprints/slack_actions.py lines 330-484 (CALLBACK_ADD_OFFICE handler, find_suggestions handler ~line 440-480)
    - lunchbot/blueprints/polls.py (any search_and_update_emoji or _seed_restaurants call site — RESEARCH cites line 184)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Seed Path Integration Points — exact edits" items 5, 6, 7
  </read_first>
  <action>
1. **Edit `lunchbot/blueprints/slack_actions.py`**:
   - Add import: `from lunchbot.services import seed_service`.
   - In the CALLBACK_ADD_OFFICE handler (lines ~359-401), after `create_workspace_location(...)` returns the new row:
     - Kick off the same background thread pattern the install flow uses (or inline if that's the current pattern) that calls `seed_service._seed_office_restaurants(current_app._get_current_object(), team_id, row['id'], lat_lng)`.
   - In `find_suggestions` (~line 440-480):
     - Line ~463 already fetches `location_row = resolve_location_for_channel(team_id, channel)`.
     - Line ~469 calls `db_client.save_restaurants(response)` — change to `db_client.save_restaurants(response, location_row['id'])`.
     - Guard: if `location_row is None`, do NOT call save_restaurants (log a warning and return the original response unchanged or an empty list — match existing error-handling style).

2. **Edit `lunchbot/blueprints/polls.py`** (if it contains a seed call):
   - RESEARCH cites line ~184 as a `search_and_update_emoji` call site. If present, thread `location_id` through: fetch the channel's bound office via `resolve_location_for_channel` and pass the `.id` as the second argument. If the call site is already wrapped by `seed_service._seed_office_restaurants` flow after Task 1, update it to call the helper instead.
   - If no such call exists in polls.py (refactor already moved it), no-op this sub-task and document in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(p).read()) for p in ['lunchbot/blueprints/slack_actions.py','lunchbot/blueprints/polls.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "seed_service._seed_office_restaurants\|_seed_office_restaurants" lunchbot/blueprints/slack_actions.py` returns at least 1
    - `grep -c "save_restaurants(response, location_row\['id'\])" lunchbot/blueprints/slack_actions.py` returns 1
    - `grep "search_and_update_emoji" lunchbot/blueprints/polls.py` — every match either passes a second positional argument (location_id) OR is deleted
    - Both files ast.parse successfully
  </acceptance_criteria>
  <done>Add-office modal seeds restaurants tagged with the new location_id. find_suggestions tags its saves. No search_and_update_emoji call site is missing location_id.</done>
</task>

</tasks>

<verification>
ast.parse + grep acceptance above.
</verification>

<success_criteria>
Both seed origins (install + add-office) call the shared helper; every save_restaurants site supplies location_id.
</success_criteria>

<output>
Create `.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-03-SUMMARY.md`
</output>
