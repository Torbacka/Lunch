---
phase: 07.2
plan: 05
type: execute
wave: 3
depends_on: [07.2-02]
files_modified:
  - lunchbot/services/recommendation_service.py
  - lunchbot/services/poll_service.py
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "ensure_poll_options accepts channel_id and threads it into db_client.get_candidate_pool and db_client.upsert_suggestion"
    - "poll_service.push_poll passes channel_id into ensure_poll_options"
    - "update_stats_lazy reads polls.slack_channel_id and writes restaurant_stats keyed on (channel_id, restaurant_id) with team_id"
    - "Polls with a NULL slack_channel_id are skipped with a warning log (cannot happen after plan 01 enforces NOT NULL, but the code must be defensive)"
  artifacts:
    - path: "lunchbot/services/recommendation_service.py"
      provides: "Channel-threaded poll option building + per-channel stats writes"
    - path: "lunchbot/services/poll_service.py"
      provides: "push_poll supplies channel_id to ensure_poll_options"
  key_links:
    - from: "poll_service.push_poll"
      to: "recommendation_service.ensure_poll_options"
      via: "channel_id kwarg"
      pattern: "ensure_poll_options\\(.*channel_id"
---

<objective>
Thread `channel_id` through the poll-build / reputation-write path so every `upsert_suggestion` call carries the channel and every `update_restaurant_stats` call keys on it.

Purpose: Connects db_client changes (plan 02) to the runtime; this is the actual fix for G-02 at the service layer.
Output: Updated recommendation_service.py + poll_service.py.
</objective>

<execution_context>
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md
@lunchbot/services/recommendation_service.py
@lunchbot/services/poll_service.py
@lunchbot/client/db_client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ensure_poll_options, update_stats_lazy, push_poll threading</name>
  <files>lunchbot/services/recommendation_service.py, lunchbot/services/poll_service.py</files>
  <read_first>
    - lunchbot/services/recommendation_service.py full (ensure_poll_options ~159, update_stats_lazy ~107-115, get_candidate_pool call ~172, upsert_suggestion call ~197)
    - lunchbot/services/poll_service.py lines 180-229 (push_poll and its ensure_poll_options call at ~220)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md sections "Reputation Job Location", "Call-chain threading"
    - lunchbot/client/db_client.py new signatures from plan 07.2-02
  </read_first>
  <behavior>
    - ensure_poll_options(poll_date, workspace_id, channel_id): must call get_candidate_pool(poll_date, channel_id) and upsert_suggestion(poll_date, restaurant_id, workspace_id, channel_id) for each selected restaurant.
    - update_stats_lazy(poll_date): iterates get_unprocessed_polls(); for each poll, reads poll['slack_channel_id'] and keys all update_restaurant_stats calls on that channel_id with team_id=poll['workspace_id']. Null channels logged and skipped via mark_poll_stats_processed.
    - poll_service.push_poll(channel, team_id, trigger_source='manual'): its call to ensure_poll_options includes channel_id=channel.
  </behavior>
  <action>
1. **lunchbot/services/recommendation_service.py**:

   a. `ensure_poll_options` — change signature to `def ensure_poll_options(poll_date, workspace_id, channel_id):` (all required). At the top, call `update_stats_lazy(poll_date)` (unchanged). Then:
      - Replace `db_client.get_candidate_pool(poll_date)` with `db_client.get_candidate_pool(poll_date, channel_id)`.
      - Every `db_client.upsert_suggestion(poll_date, restaurant_id, workspace_id=workspace_id)` call becomes `db_client.upsert_suggestion(poll_date, restaurant_id, workspace_id, channel_id)`.

   b. `update_stats_lazy(poll_date)` — change body to:
```python
def update_stats_lazy(poll_date):
    unprocessed = db_client.get_unprocessed_polls(poll_date)
    for poll in unprocessed:
        channel_id = poll.get('slack_channel_id')
        workspace_id = poll['workspace_id']
        if not channel_id:
            logger.warning('poll %s has no slack_channel_id, skipping stats update', poll['id'])
            db_client.mark_poll_stats_processed(poll['id'])
            continue
        vote_shares = db_client.get_poll_vote_shares(poll['id'])
        for vs in vote_shares:
            alpha_inc = vs['alpha_increment']
            beta_inc = vs['beta_increment']
            db_client.update_restaurant_stats(
                channel_id,
                vs['restaurant_id'],
                alpha_inc,
                beta_inc,
                workspace_id,
            )
        db_client.mark_poll_stats_processed(poll['id'])
```
Adapt field names (`alpha_increment`, `beta_increment`, `get_poll_vote_shares`, `mark_poll_stats_processed`) to whatever the current code uses — do not invent names. The shape is: for each unprocessed poll, key update_restaurant_stats on (channel_id from the poll row, restaurant_id from the vote share, team_id=workspace_id).

2. **lunchbot/services/poll_service.py**:

   In `push_poll(channel, team_id, trigger_source=...)`, find the `ensure_poll_options(...)` call (~line 220) and change it to `recommendation_service.ensure_poll_options(poll_date=date.today(), workspace_id=team_id, channel_id=channel)`. No other changes.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(p).read()) for p in ['lunchbot/services/recommendation_service.py','lunchbot/services/poll_service.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def ensure_poll_options(poll_date, workspace_id, channel_id)" lunchbot/services/recommendation_service.py` returns 1
    - `grep -c "get_candidate_pool(poll_date, channel_id)" lunchbot/services/recommendation_service.py` returns 1
    - `grep -c "upsert_suggestion" lunchbot/services/recommendation_service.py` >= 1 AND every match passes 4 args
    - `grep -c "poll.get('slack_channel_id')\|poll\[.slack_channel_id.\]" lunchbot/services/recommendation_service.py` >= 1
    - `grep -c "channel_id=channel" lunchbot/services/poll_service.py` returns 1
    - `grep "update_restaurant_stats" lunchbot/services/recommendation_service.py` shows calls passing channel_id as first argument
    - Both files ast.parse successfully
  </acceptance_criteria>
  <done>Channel threads through ensure_poll_options → get_candidate_pool and → upsert_suggestion; update_stats_lazy writes per-channel stats.</done>
</task>

</tasks>

<verification>
Grep + ast.parse above.
</verification>

<success_criteria>
Every poll-build path and reputation-write path carries channel_id.
</success_criteria>

<output>
Create `.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-05-SUMMARY.md`
</output>
