---
phase: 07.2-per-channel-thompson-sampling-office-scoped-candidates
plan: 02
subsystem: database
tags: [postgres, psycopg3, rls, thompson-sampling, channel-keyed]

requires:
  - phase: 07.2-01
    provides: "Migration 009 schema: restaurants.location_id, channel_schedules table, per-channel restaurant_stats"
provides:
  - "Channel-keyed db_client functions: upsert_suggestion, get_candidate_pool, update_restaurant_stats, get_or_create_stats"
  - "Office-scoped candidate pool via channel_office CTE"
  - "Channel schedules CRUD: list, get, upsert, delete"
  - "get_unprocessed_polls returns slack_channel_id"
affects: [07.2-03, 07.2-04, 07.2-05, 07.2-06, 07.2-07]

tech-stack:
  added: []
  patterns: ["channel_office CTE for office-scoped queries", "channel_id+restaurant_id composite key for stats"]

key-files:
  created: ["tests/test_db_client_channel_keyed.py"]
  modified: ["lunchbot/client/db_client.py"]

key-decisions:
  - "All channel_schedules CRUD use direct pool connection with SET app.current_tenant (not execute_with_tenant) matching existing db_client patterns for write operations"
  - "save_restaurant uses COALESCE(restaurants.location_id, EXCLUDED.location_id) to preserve existing location on re-upsert"

patterns-established:
  - "channel_office CTE: resolve channel_id to location_id via channel_locations for office-scoped queries"
  - "Per-channel stats: all restaurant_stats operations keyed on (channel_id, restaurant_id) with team_id for RLS"

requirements-completed: [SC-1, SC-2, SC-3, SC-4, SC-6]

duration: 3min
completed: 2026-04-16
---

# Phase 07.2 Plan 02: DB Client Channel-Keyed Rewrite Summary

**Rewrote db_client for per-channel Thompson sampling stats, office-scoped candidate pools via channel_office CTE, and channel_schedules CRUD**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-16T04:58:31Z
- **Completed:** 2026-04-16T05:01:57Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 2

## Accomplishments
- Rewrote 6 db_client functions to be channel-keyed: upsert_suggestion, save_restaurant, save_restaurants, get_candidate_pool, get_or_create_stats, update_restaurant_stats
- Added channel_office CTE in get_candidate_pool for office-scoped restaurant filtering (fixes G-01)
- Re-keyed restaurant_stats operations on (channel_id, restaurant_id) instead of (restaurant_id, workspace_id) (fixes G-02)
- Added 4 new channel_schedules CRUD functions (D-12)
- Updated get_unprocessed_polls to return slack_channel_id
- No references to dropped workspaces columns remain

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Failing tests for channel-keyed signatures** - `e154735` (test)
2. **Task 1 GREEN: Implement channel-keyed db_client rewrites** - `ed3906b` (feat)

## Files Created/Modified
- `lunchbot/client/db_client.py` - Rewrote 6 functions for channel-keyed stats/office-scoped pools, added 4 channel_schedules CRUD helpers
- `tests/test_db_client_channel_keyed.py` - 10 signature and source inspection tests for new contracts

## Decisions Made
- Used direct pool connection with SET app.current_tenant for channel_schedules CRUD (consistent with existing write-path patterns in db_client)
- save_restaurant COALESCE preserves existing location_id on conflict to avoid overwriting a correct location with a stale one

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All downstream plans (03-08) can now import the new db_client signatures
- Existing tests in test_db.py and test_recommendation_db.py will fail until callers are updated (plans 03-07) -- this is expected and documented in the plan
- channel_schedules table must exist (migration 009 from plan 01) before CRUD functions can be called

## Self-Check: PASSED

All files exist, all commits verified (e154735, ed3906b).

---
*Phase: 07.2-per-channel-thompson-sampling-office-scoped-candidates*
*Completed: 2026-04-16*
