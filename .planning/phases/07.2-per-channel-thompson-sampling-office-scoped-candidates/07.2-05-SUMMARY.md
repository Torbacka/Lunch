---
phase: "07.2"
plan: "05"
subsystem: recommendation-engine
tags: [channel-threading, thompson-sampling, stats-write, poll-build]
dependency_graph:
  requires: [07.2-02]
  provides: [channel-scoped-poll-options, channel-scoped-stats-writes]
  affects: [recommendation_service, poll_service]
tech_stack:
  added: []
  patterns: [channel-id-threading, defensive-null-channel-skip]
key_files:
  created:
    - tests/test_channel_threading.py
  modified:
    - lunchbot/services/recommendation_service.py
    - lunchbot/services/poll_service.py
    - tests/test_recommendation.py
decisions:
  - "update_stats_lazy reads workspace_id from poll row instead of Flask g context, making it explicit per-poll"
  - "NULL slack_channel_id polls are skipped with warning and marked processed to avoid reprocessing loops"
  - "update_restaurant_stats called with channel_id as first positional arg matching plan 02 signature convention"
metrics:
  duration_seconds: 189
  completed: "2026-04-16T05:09:16Z"
  tasks_completed: 1
  tasks_total: 1
  files_modified: 4
---

# Phase 07.2 Plan 05: Channel-ID Threading Through Poll-Build and Stats-Write Paths

Channel_id threaded through ensure_poll_options, update_stats_lazy, and push_poll so every db_client call carries the Slack channel for per-channel Thompson sampling scoping.

## What Was Done

### Task 1: Rewrite ensure_poll_options, update_stats_lazy, push_poll threading (TDD)

**RED:** 7 failing tests covering:
- `ensure_poll_options` signature accepts `channel_id`
- `get_candidate_pool` receives `(poll_date, channel_id)`
- `upsert_suggestion` receives `channel_id` as 4th positional arg
- `update_stats_lazy` reads `slack_channel_id` from poll rows
- NULL `slack_channel_id` polls skipped with warning log
- `workspace_id` passed from poll row to `update_restaurant_stats`
- `push_poll` passes `channel_id=channel` to `ensure_poll_options`

**GREEN:** All 7 new tests + 42 existing tests pass (49 total).

**Changes:**
- `recommendation_service.ensure_poll_options(poll_date, workspace_id, channel_id)` -- all required args, threads channel_id into `get_candidate_pool` and `upsert_suggestion`
- `recommendation_service.update_stats_lazy` -- reads `slack_channel_id` and `workspace_id` from each poll row, passes `channel_id` as first arg to `update_restaurant_stats`, skips NULL channels with warning
- `poll_service.push_poll` -- passes `workspace_id=team_id, channel_id=channel` to `ensure_poll_options`
- Updated existing test mock to match new 3-arg signature

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing test mock signature**
- **Found during:** Task 1 GREEN phase
- **Issue:** `test_ensure_poll_options_called_from_push_poll` in `test_recommendation.py` used old 2-arg mock signature `(poll_date=None, workspace_id=None)`
- **Fix:** Updated mock to accept `channel_id=None` and added assertions for `workspace_id` and `channel_id` values
- **Files modified:** tests/test_recommendation.py
- **Commit:** 0d500d6

## Verification

| Criterion | Result |
|-----------|--------|
| `def ensure_poll_options(poll_date, workspace_id, channel_id)` | PASS (1 match) |
| `get_candidate_pool(poll_date, channel_id)` | PASS (1 match) |
| `upsert_suggestion` passes 4 positional args | PASS |
| `poll.get('slack_channel_id')` in update_stats_lazy | PASS (1 match) |
| `channel_id=channel` in poll_service | PASS (1 match) |
| `update_restaurant_stats` called with channel_id first | PASS |
| Both files ast.parse successfully | PASS |
| 49 tests pass | PASS |

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 (RED) | ae3f66c | test(07.2-05): add failing tests for channel_id threading |
| 1 (GREEN) | 0d500d6 | feat(07.2-05): thread channel_id through poll-build and stats-write paths |
