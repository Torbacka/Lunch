---
phase: "07.2"
plan: "06"
subsystem: ui
tags: [slack-app-home, block-kit, channel-schedule, d-14, d-16]

requires:
  - phase: 07.2-02
    provides: "channel_schedules CRUD in db_client"
  - phase: 07.2-04
    provides: "Channel-keyed scheduler_service.update_schedule_job(team_id, channel_id, ...)"
provides:
  - "Per-channel schedule rendering in App Home"
  - "Channel-scoped schedule modal (create + edit)"
  - "list_bot_channels Slack API wrapper"
affects: [07.2-07]

tech-stack:
  added: []
  patterns: ["per-channel schedule list in App Home", "static_select channel picker from list_bot_channels"]

key-files:
  created: []
  modified:
    - lunchbot/client/slack_client.py
    - lunchbot/services/app_home_service.py
    - lunchbot/blueprints/slack_actions.py
    - lunchbot/blueprints/events.py
    - lunchbot/client/db_client.py

key-decisions:
  - "Begin Setup button now opens the schedule-channel modal instead of the old channel picker"
  - "Channel picker uses static_select from list_bot_channels (not conversations_select) to match D-16"
  - "Edit mode shows channel as read-only text with channel_id in private_metadata"

requirements-completed: [SC-7]

duration: 5m 25s
completed: 2026-04-16
---

# Phase 07.2 Plan 06: App Home Per-Channel Schedule UI Summary

Per-channel schedule list replacing workspace-level Poll Channel + Poll Schedule sections, with channel-picker modal backed by list_bot_channels and channel_schedules CRUD

## Performance

- **Duration:** 5 min 25 sec
- **Started:** 2026-04-16T05:14:56Z
- **Completed:** 2026-04-16T05:20:21Z
- **Tasks:** 3/3
- **Files modified:** 5

## Accomplishments

- Added `list_bot_channels(team_id, cursor)` to `slack_client.py` wrapping `users.conversations` for D-16 bot-member channel enumeration
- Deleted `_build_channel_section` (Poll Channel) and `_build_schedule_section` (workspace-level Poll Schedule) from App Home
- Added `_build_channel_schedules_section` rendering per-channel rows as `#channel -- HH:MM TZ -- weekdays` with Edit buttons
- Added `build_schedule_channel_modal` supporting both create (with channel picker) and edit (channel read-only) modes
- Rewrote CALLBACK_SCHEDULE handler to CALLBACK_SCHEDULE_CHANNEL: parses channel from select or metadata, calls `db_client.upsert_channel_schedule` + `scheduler_service.update_schedule_job(team_id, channel_id, ...)`
- Removed old CALLBACK_CHANNEL, CALLBACK_REMOVE_SCHEDULE, and all `poll_schedule_*` / `update_poll_channel` code paths
- Updated all `build_home_view` callers (events.py, slack_actions._refresh_app_home) to pass `schedules` parameter

## Task Commits

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Add slack_client.list_bot_channels wrapper | 9bbf53a | lunchbot/client/slack_client.py |
| 2 | Rewrite App Home rendering -- per-channel schedule list | 914c34d | lunchbot/services/app_home_service.py, lunchbot/blueprints/events.py, lunchbot/client/db_client.py |
| 3 | Rewrite CALLBACK_SCHEDULE handlers for channel-scoped modal | 70faf5a | lunchbot/blueprints/slack_actions.py |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added channel_schedules CRUD to db_client**
- **Found during:** Task 2
- **Issue:** Plan 02 ran in parallel worktree; `list_channel_schedules`, `get_channel_schedule`, `upsert_channel_schedule`, `delete_channel_schedule` not present in this worktree
- **Fix:** Added all 4 CRUD functions to db_client.py matching the plan 02 specification
- **Files modified:** lunchbot/client/db_client.py
- **Commit:** 914c34d

## Known Stubs

None -- all functions are wired to real data sources.

## Self-Check: PASSED
