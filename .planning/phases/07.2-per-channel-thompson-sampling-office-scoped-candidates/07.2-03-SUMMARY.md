---
phase: 07.2
plan: 03
subsystem: restaurant-seeding
tags: [seed-service, location-tagging, shared-helper]
dependency_graph:
  requires: [07.2-01, 07.2-02]
  provides: [shared-seed-helper, location-tagged-restaurants]
  affects: [setup.py, slack_actions.py, polls.py, emoji_service.py]
tech_stack:
  added: []
  patterns: [shared-service-helper, background-thread-seeding]
key_files:
  created:
    - lunchbot/services/seed_service.py
  modified:
    - lunchbot/services/emoji_service.py
    - lunchbot/blueprints/setup.py
    - lunchbot/blueprints/slack_actions.py
    - lunchbot/blueprints/polls.py
decisions:
  - Kept background threading pattern from setup.py for seed calls in slack_actions.py
  - Made location_id and team_id optional params in search_and_update_emoji for backward compat
metrics:
  duration: 189s
  completed: 2026-04-16T05:08:36Z
---

# Phase 07.2 Plan 03: Shared Seed Helper and Location-Tagged Seeding Summary

Extracted install-flow restaurant seeding into seed_service._seed_office_restaurants shared helper, wired both install and add-office modal to use it, threaded location_id through all save_restaurants call sites.

## Task Results

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Extract shared seed helper and wire install flow | 7317c79 | Done |
| 2 | Wire add-office modal + find_suggestions + polls.py | 54bdd7c | Done |

## What Was Done

### Task 1: Extract shared seed helper and wire install flow
- Created `lunchbot/services/seed_service.py` with `_seed_office_restaurants(app, team_id, location_id, lat_lng)` helper
- Updated `emoji_service.search_and_update_emoji` signature to accept `location_id` and `team_id` params
- Changed `db_client.save_restaurants` call in emoji_service to pass `location_id`
- Replaced setup.py's internal `_seed_restaurants` with a call to `seed_service._seed_office_restaurants`, passing `row['id']`
- Removed unused `g` import from setup.py

### Task 2: Wire add-office modal + find_suggestions + polls.py
- Added `threading` import and `seed_service` import to slack_actions.py
- Added background thread seed call in CALLBACK_ADD_OFFICE handler after `create_workspace_location`
- Updated `find_suggestions` endpoint to pass `location_row['id']` to `db_client.save_restaurants`
- Updated polls.py `/seed` endpoint to pass `location_row['id']` and `team_id` to `search_and_update_emoji`

## Deviations from Plan

None - plan executed exactly as written.

## Decisions Made

1. **Optional params for backward compatibility**: Made `location_id` and `team_id` default to `None` in `search_and_update_emoji` so any existing callers without location context still work.
2. **Preserved background thread pattern**: Used the same `threading.Thread(daemon=True)` pattern from setup.py for the add-office modal seed call.
