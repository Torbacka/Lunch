---
phase: 07.2
plan: 08
type: execute
wave: 6
depends_on: [07.2-01, 07.2-02, 07.2-03, 07.2-04, 07.2-05, 07.2-06, 07.2-07]
files_modified: []
autonomous: true
requirements: [SC-3, SC-8]
must_haves:
  truths:
    - "Migration 009 has been applied against the dev/test PostgreSQL instance via `alembic upgrade head`"
    - "After the upgrade, the full test suite passes green"
    - "A live candidate-pool smoke query against a seeded two-office fixture returns office-scoped results"
  artifacts: []
  key_links:
    - from: "alembic upgrade head"
      to: "live database schema"
      via: "migration 009 applied"
      pattern: ""
---

<objective>
[BLOCKING] Apply migration 009 against the dev/test database and run the full test suite end-to-end to prove the schema + code + tests are coherent.

Purpose: Without this step, test runs use stale schema and produce false positives. This is the phase gate.
Output: Green test suite against migrated DB.
</objective>

<execution_context>
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md
@migrations/versions/009_office_scoped_candidates_and_channel_schedules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration 009 and run full test suite</name>
  <files></files>
  <read_first>
    - migrations/versions/009_office_scoped_candidates_and_channel_schedules.py (verify file exists and chains from 008)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Migration ordering note"
  </read_first>
  <action>
1. Verify alembic can see revision 009:
   ```
   alembic history | grep "009\|008"
   ```
   Expect both 008 and 009 listed with 009 as head.

2. Apply the migration:
   ```
   alembic upgrade head
   ```
   If the command fails due to orphan restaurants (Step A3 RAISE EXCEPTION), investigate the dev DB state — do NOT edit the migration to soften the check. Fix the data or document the failure and escalate.

3. Verify post-upgrade invariants directly against the DB using psql or the project's existing `scripts/` helpers:
   ```
   psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM restaurants WHERE location_id IS NULL"
   # expect 0
   psql "$DATABASE_URL" -c "\\d restaurant_stats"
   # expect PRIMARY KEY (channel_id, restaurant_id); NO workspace_id column
   psql "$DATABASE_URL" -c "\\d polls" | grep slack_channel_id
   # expect "not null"
   psql "$DATABASE_URL" -c "\\d channel_schedules"
   # expect table exists with PK (team_id, channel_id)
   psql "$DATABASE_URL" -c "\\d workspaces" | grep -E "poll_schedule|poll_channel"
   # expect NO matches
   ```

4. Run the full test suite:
   ```
   pytest -x
   ```
   Expect exit code 0. Any failure here means plans 02-07 missed a call site — fix forward via gap closure; do not mutate migration 009.

5. Run a live candidate-pool smoke check via a short Python snippet against the dev DB (optional but recommended): insert two workspace_locations + two channel_locations + two restaurants tagged with different location_ids, call `db_client.get_candidate_pool(today, 'C_A')`, assert the returned place_ids are exclusively the office-A set. This replicates the G-01 fix against live infra.
  </action>
  <verify>
    <automated>alembic upgrade head && pytest -x --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `alembic current` shows revision `009` as the head
    - `psql "$DATABASE_URL" -tc "SELECT COUNT(*) FROM restaurants WHERE location_id IS NULL"` returns 0
    - `psql "$DATABASE_URL" -tc "SELECT COUNT(*) FROM information_schema.columns WHERE table_name='restaurant_stats' AND column_name='workspace_id'"` returns 0
    - `psql "$DATABASE_URL" -tc "SELECT is_nullable FROM information_schema.columns WHERE table_name='polls' AND column_name='slack_channel_id'"` returns `NO`
    - `psql "$DATABASE_URL" -tc "SELECT 1 FROM information_schema.tables WHERE table_name='channel_schedules'"` returns 1
    - `psql "$DATABASE_URL" -tc "SELECT COUNT(*) FROM information_schema.columns WHERE table_name='workspaces' AND column_name IN ('poll_channel','poll_schedule_time','poll_schedule_timezone','poll_schedule_weekdays')"` returns 0
    - `pytest -x` exits 0
  </acceptance_criteria>
  <done>Migration applied; full suite green; phase 07.2 invariants verified against live schema.</done>
</task>

</tasks>

<verification>
alembic + psql + pytest acceptance criteria above.
</verification>

<success_criteria>
Phase gate: green suite on migrated DB. Ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-08-SUMMARY.md`
</output>
