---
phase: 07.2
plan: 06
type: execute
wave: 4
depends_on: [07.2-02, 07.2-04]
files_modified:
  - lunchbot/services/app_home_service.py
  - lunchbot/blueprints/slack_actions.py
  - lunchbot/client/slack_client.py
autonomous: true
requirements: [SC-7]
must_haves:
  truths:
    - "App Home no longer shows a Poll Channel section"
    - "App Home no longer shows a workspace-level Poll Schedule section"
    - "App Home shows a per-channel schedule list where each row displays `#channel — HH:MM TZ — weekdays` with an Edit button"
    - "App Home shows a 'Schedule a channel' button that opens a modal with a channel picker, time input, timezone select, weekday multi-select"
    - "The channel picker lists only channels the bot is a member of (via slack_client.list_bot_channels wrapping users.conversations)"
    - "Submitting the schedule modal inserts/updates a channel_schedules row and calls scheduler_service.update_schedule_job(team_id, channel_id, ...)"
    - "CALLBACK_SCHEDULE path in slack_actions.py is rewritten accordingly"
  artifacts:
    - path: "lunchbot/services/app_home_service.py"
      provides: "Per-channel schedule rendering in App Home"
    - path: "lunchbot/blueprints/slack_actions.py"
      provides: "CALLBACK_SCHEDULE handler for channel-scoped schedule modal"
    - path: "lunchbot/client/slack_client.py"
      provides: "list_bot_channels(team_id, cursor=None)"
  key_links:
    - from: "schedule modal submission"
      to: "scheduler_service.update_schedule_job"
      via: "direct call after db upsert"
      pattern: "update_schedule_job\\(team_id, channel_id"
---

<objective>
Replace the two legacy workspace-level App Home sections (Poll Channel, Poll Schedule) with a per-channel schedule list and a channel-picker modal. Add a Slack client wrapper for enumerating bot-member channels.

Purpose: Delivers D-14 and D-16 — the user-visible UX of channel-scoped scheduling.
Output: Updated app_home_service, slack_actions, and slack_client.
</objective>

<execution_context>
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel.torbacka/dev/private/Lunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md
@.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md
@lunchbot/services/app_home_service.py
@lunchbot/blueprints/slack_actions.py
@lunchbot/client/slack_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slack_client.list_bot_channels wrapper</name>
  <files>lunchbot/client/slack_client.py</files>
  <read_first>
    - lunchbot/client/slack_client.py (existing style: how tokens are fetched per team, how other API wrappers are shaped, session pattern)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Channel picker in App Home modal (D-14, D-16)"
  </read_first>
  <action>
Add a function `list_bot_channels(team_id, cursor=None)` to `lunchbot/client/slack_client.py`. It must:

1. Fetch the workspace's bot token via the existing per-team token helper (use whatever pattern the existing functions use).
2. Call Slack Web API `users.conversations` with parameters:
   - `types=public_channel,private_channel`
   - `exclude_archived=true`
   - `limit=200`
   - `cursor=<cursor>` if supplied
3. Return a tuple `(channels, next_cursor)` where `channels` is a list of dicts each containing at minimum `id` and `name`, and `next_cursor` is the `response_metadata.next_cursor` or `None` when empty.
4. Use the existing module-level `session` variable; follow the same error-handling style as neighbouring functions (log the status_code + body, raise on non-2xx the same way the existing code does, or return `([], None)` on error if that matches the convention — match the file).

Snake_case, no type hints. Docstring on the function describing the D-16 purpose (bot-member channels only, which `users.conversations` already guarantees).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('lunchbot/client/slack_client.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def list_bot_channels" lunchbot/client/slack_client.py` returns 1
    - `grep -c "users.conversations" lunchbot/client/slack_client.py` returns 1
    - `grep -c "next_cursor" lunchbot/client/slack_client.py` >= 1
    - File ast.parses successfully
  </acceptance_criteria>
  <done>Bot-channel enumeration helper available to App Home modal code.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite App Home rendering — remove workspace schedule sections, add per-channel list</name>
  <files>lunchbot/services/app_home_service.py</files>
  <read_first>
    - lunchbot/services/app_home_service.py full (current `build_home_view`, `_build_channel_section` ~128-144, `_build_schedule_section` ~146-201, `_build_offices_section` ~447-507)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "App Home current layout"
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md D-14
  </read_first>
  <action>
Edit `lunchbot/services/app_home_service.py`:

1. **Delete** `_build_channel_section(...)` (the Poll Channel section, ~lines 128-144) entirely. Remove its call site inside `build_home_view`.

2. **Delete** `_build_schedule_section(...)` (the workspace-level Poll Schedule section, ~lines 146-201) entirely. Remove its call site.

3. **Add** new function `_build_channel_schedules_section(schedules)`:
   - `schedules` is a list of dicts like `{'channel_id': 'C123', 'schedule_time': time(12,0), 'schedule_timezone': 'Europe/Stockholm', 'schedule_weekdays': 'mon,tue,wed,thu,fri'}`.
   - Emit a header block `Per-channel poll schedules`.
   - For each schedule, emit a section block with text like `<#{channel_id}> — {HH:MM} {timezone} — {weekdays}` and an accessory button `{"type":"button","text":{"type":"plain_text","text":"Edit"},"action_id":"edit_channel_schedule","value":"{channel_id}"}`.
   - After the list, emit one actions block with a button `{"type":"button","text":{"type":"plain_text","text":"Schedule a channel"},"action_id":"open_schedule_channel_modal"}`.
   - If `schedules` is empty, render only an explanatory context block ("No channel schedules yet — click Schedule a channel to add one.") plus the Schedule a channel button.

4. **Update** `build_home_view(settings, is_admin, locations, schedules)` signature: add `schedules` parameter. Inside, where the deleted sections used to live, call `_build_channel_schedules_section(schedules)` and extend the blocks list. Keep Poll Size section + Offices section intact.

5. **Update all callers** of `build_home_view` in the codebase (search for `build_home_view(`) to fetch schedules via `db_client.list_channel_schedules(team_id)` and pass them in. These call sites are in `lunchbot/blueprints/events.py` (app_home_opened) and `lunchbot/blueprints/slack_actions.py` (after settings change). Update both.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('lunchbot/services/app_home_service.py').read())" && grep -rn "build_home_view" lunchbot/ | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_build_channel_section\|_build_schedule_section" lunchbot/services/app_home_service.py` returns 0
    - `grep -c "def _build_channel_schedules_section" lunchbot/services/app_home_service.py` returns 1
    - `grep -c "open_schedule_channel_modal" lunchbot/services/app_home_service.py` returns 1
    - `grep -c "edit_channel_schedule" lunchbot/services/app_home_service.py` returns 1
    - `grep -c "def build_home_view" lunchbot/services/app_home_service.py` returns 1 AND its signature includes `schedules`
    - Every caller of `build_home_view(` passes a `schedules` argument (manual grep review — no bare `build_home_view(settings, is_admin, locations)` calls remain)
    - File ast.parses successfully
  </acceptance_criteria>
  <done>App Home shows per-channel schedule list; workspace-level sections are gone; all callers updated.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite CALLBACK_SCHEDULE handlers for channel-scoped modal</name>
  <files>lunchbot/blueprints/slack_actions.py</files>
  <read_first>
    - lunchbot/blueprints/slack_actions.py (current CALLBACK_SCHEDULE handler ~line 297, any open-schedule-modal action handler)
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-RESEARCH.md section "Call-site inventory for the restructure"
    - .planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-CONTEXT.md D-14, D-16
  </read_first>
  <action>
Edit `lunchbot/blueprints/slack_actions.py`:

1. **Action handler for `open_schedule_channel_modal`** (button from App Home, Task 2):
   - Call `slack_client.list_bot_channels(team_id)` to fetch member channels.
   - Build a modal view with:
     - `callback_id = "schedule_channel_modal"`
     - `blocks`:
       - A `static_select` named `schedule_channel` with options built from list_bot_channels result, label "Channel"
       - A `timepicker` named `schedule_time`, label "Time"
       - A `static_select` named `schedule_timezone`, label "Timezone" — options list matches the timezone list currently used by the deleted workspace schedule modal (read the pre-edit source before deleting to preserve the exact list)
       - A `multi_static_select` named `schedule_weekdays` with options `Monday..Sunday` (values `mon,tue,wed,thu,fri,sat,sun`), label "Weekdays"
     - `submit` text "Save"
   - Open via `views.open` using the Slack client wrapper the codebase already uses (match existing modal-open call sites).

2. **Action handler for `edit_channel_schedule`** (Edit button per row):
   - `action.value` contains the channel_id.
   - Fetch existing schedule via `db_client.get_channel_schedule(team_id, channel_id)`.
   - Open the same modal as above but with `initial_*` values populated from the existing row and the channel picker replaced with a read-only `section` block showing the channel name (channel cannot be changed via edit — only time/tz/weekdays).
   - Store the target channel_id in `private_metadata` of the view so submission can read it back.

3. **CALLBACK_SCHEDULE submission handler** (view_submission for `callback_id = schedule_channel_modal`):
   - Parse values: `channel_id` from either the static_select state or `private_metadata`.
   - Parse `schedule_time` (time string), `schedule_timezone`, `schedule_weekdays` (list → comma-joined).
   - Call `db_client.upsert_channel_schedule(team_id, channel_id, schedule_time, schedule_timezone, schedule_weekdays)`.
   - Call `scheduler_service.update_schedule_job(team_id, channel_id, schedule_time, schedule_timezone, schedule_weekdays)`.
   - Re-publish the App Home view for the submitting user (fetch updated schedules via `db_client.list_channel_schedules(team_id)` and call `build_home_view` + `views.publish`).

4. **Delete** the old workspace-level schedule form submission handler (the one that wrote to `workspaces.poll_schedule_*`). Its callback_id was the previous CALLBACK_SCHEDULE — either repurpose the name or create a new one; either way, the old column-writing path must be removed.

5. **Delete** any code path that called `workspace_client.update_poll_channel(...)` or equivalent (the old Poll Channel section submission).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('lunchbot/blueprints/slack_actions.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "schedule_channel_modal" lunchbot/blueprints/slack_actions.py` >= 2
    - `grep -c "open_schedule_channel_modal" lunchbot/blueprints/slack_actions.py` >= 1
    - `grep -c "edit_channel_schedule" lunchbot/blueprints/slack_actions.py` >= 1
    - `grep -c "list_bot_channels" lunchbot/blueprints/slack_actions.py` >= 1
    - `grep -c "upsert_channel_schedule" lunchbot/blueprints/slack_actions.py` >= 1
    - `grep -c "update_schedule_job(team_id, channel_id" lunchbot/blueprints/slack_actions.py` >= 1
    - `grep "update_poll_channel\|poll_schedule_time\|poll_schedule_timezone\|poll_schedule_weekdays" lunchbot/blueprints/slack_actions.py` returns no matches
    - File ast.parses successfully
  </acceptance_criteria>
  <done>Channel-scoped schedule modal end-to-end: open → pick channel → save → scheduler updated → App Home re-rendered.</done>
</task>

</tasks>

<verification>
Grep + ast.parse above. UI flow validated end-to-end via test plan 07.2-07.
</verification>

<success_criteria>
D-14 and D-16 implemented; workspace-level UI gone.
</success_criteria>

<output>
Create `.planning/phases/07.2-per-channel-thompson-sampling-office-scoped-candidates/07.2-06-SUMMARY.md`
</output>
