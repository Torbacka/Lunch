---
phase: 07.2
plan: 07
subsystem: tests
tags: [regression, g-01, g-02, migration-009, scheduler, app-home, channel-picker]
dependency_graph:
  requires: [07.2-02, 07.2-03, 07.2-04, 07.2-05, 07.2-06]
  provides: [g-01-regression-test, g-02-regression-test, migration-009-smoke-test, channel-keyed-scheduler-test]
  affects: [tests/test_recommendation_db.py, tests/test_db.py, tests/test_migration_009.py, tests/test_scheduler.py, tests/test_app_home.py, tests/test_slack_actions.py]
tech_stack:
  added: []
  patterns: [per-channel test helpers, schema introspection assertions]
key_files:
  created:
    - tests/test_migration_009.py
    - tests/test_scheduler.py
    - tests/test_slack_actions.py
  modified:
    - tests/test_recommendation_db.py
    - tests/test_db.py
    - tests/test_app_home.py
decisions:
  - Created separate test_scheduler.py for channel-keyed tests rather than extending legacy test_scheduler_service.py (old tests use pre-07.2 API signatures)
  - Adapted test_add_office_modal_seeds_with_location to test create_workspace_location + bind_channel_location (actual code path) rather than seed_service._seed_office_restaurants (only called from install flow)
metrics:
  duration: 629s
  completed: 2026-04-16T05:34:14Z
  tasks_completed: 2
  tasks_total: 2
  files_created: 3
  files_modified: 3
---

# Phase 07.2 Plan 07: Regression + Integration Test Suite Summary

Full regression and integration test suite proving all Phase 07.2 acceptance criteria hold: G-01 office isolation, G-02 per-channel stats divergence, migration 009 invariants, channel-keyed scheduler jobs, per-channel App Home rendering, and channel picker filtering.

## Task 1: Test fixtures + G-01/G-02/Beta fallback/cascade regression tests

**Commit:** d7b6600

Rewrote `tests/test_recommendation_db.py` with new per-channel helpers and updated all existing tests to use the migration 009 schema:

- `_insert_workspace_location()` and `_insert_channel_location()` helpers
- `_insert_restaurant()` now requires `location_id`; `_insert_poll()` now requires `slack_channel_id`
- 6 existing tests updated to use per-channel `get_or_create_stats(channel_id, restaurant_id, team_id)`, `get_candidate_pool(poll_date, channel_id)`, `update_restaurant_stats(channel_id, ...)`

New regression tests:
- `test_get_candidate_pool_office_isolation` -- G-01: two offices, two channels, disjoint pools, `isdisjoint` assertion
- `test_restaurant_stats_divergence_same_office` -- G-02: same office, two channels, divergent alpha/beta
- `test_get_candidate_pool_new_channel_prior` -- Beta(1,1) fallback for channel with no stats
- `test_delete_office_cascades_restaurants_and_stats` -- D-05: CASCADE from workspace_locations

In `tests/test_db.py`:
- Updated `save_restaurant` calls to pass `location_id`
- Updated `upsert_suggestion` calls to pass `slack_channel_id`
- Added `test_upsert_suggestion_requires_channel_id` -- TypeError when 4th arg omitted

## Task 2: Migration 009 smoke test + scheduler + App Home + channel picker

**Commit:** 4913fbc

**tests/test_migration_009.py** (new):
- `test_restaurants_location_id_not_null` -- schema column assertion
- `test_restaurant_stats_primary_key` -- PK is (channel_id, restaurant_id), no workspace_id
- `test_polls_slack_channel_id_not_null` -- NOT NULL assertion
- `test_channel_schedules_exists` -- table exists, PK is (team_id, channel_id)
- `test_workspaces_schedule_columns_dropped` -- legacy columns removed

**tests/test_scheduler.py** (new):
- `test_load_all_schedules_creates_channel_keyed_jobs` -- seeds channel_schedules, asserts `poll_T1_C1` job
- `test_update_schedule_job_upserts_job` -- verifies trigger weekday match
- `test_remove_schedule_job_removes_job` -- add then remove, assert None

**tests/test_app_home.py** (extended):
- `test_build_home_view_renders_per_channel_schedules` -- asserts `<#C1>`, `<#C2>`, `open_schedule_channel_modal`, no legacy sections
- `test_build_home_view_no_workspace_schedule_sections` -- empty schedules, no Poll Channel/Schedule text

**tests/test_slack_actions.py** (new):
- `test_list_bot_channels_filters_to_members` -- D-16: `users.conversations` endpoint, NOT `conversations.list`, 3 channels returned
- `test_add_office_modal_seeds_with_location` -- verifies create_workspace_location + bind_channel_location called with location_id

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created test_scheduler.py separate from test_scheduler_service.py**
- **Found during:** Task 2
- **Issue:** Existing test_scheduler_service.py uses pre-07.2 API signatures (e.g. `update_schedule_job(..., channel='C1')`, `remove_schedule_job('T_REM')` with one arg). Extending it would require rewriting all existing tests.
- **Fix:** Created new test_scheduler.py with channel-keyed tests; legacy file left untouched for plan 07.2-08 migration to handle.
- **Files modified:** tests/test_scheduler.py (new)
- **Commit:** 4913fbc

**2. [Rule 1 - Bug] Adapted test_add_office_modal_seeds_with_location**
- **Found during:** Task 2
- **Issue:** Plan specified monkeypatching `seed_service._seed_office_restaurants`, but that function is only called from `setup.py` (install flow), not from the add-office modal in `slack_actions.py`.
- **Fix:** Test verifies the actual code path: `create_workspace_location` + `bind_channel_location(team_id, channel_id, location_id)`.
- **Files modified:** tests/test_slack_actions.py (new)
- **Commit:** 4913fbc

## Self-Check: PASSED
